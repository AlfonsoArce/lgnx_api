# lgnx_api

Client/wrapper for the LogiNext API.

## Status

Early scaffolding — no implementation yet. This repository will house a client library that wraps the LogiNext REST API for use in downstream projects.

## Layout

```
.
├── README.md
└── .gitignore
```

Local-only reference material (e.g. vendor API PDFs) lives under `references/` and is gitignored.
