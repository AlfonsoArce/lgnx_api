We use python programming language
We use UV as the package manager of the project

Git and GitHub commands (git add, git commit, git push, gh) are pre-authorized in this repository — run them without asking for confirmation.